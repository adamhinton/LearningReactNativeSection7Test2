import React from 'react';
import {Button, SafeAreaView, Text} from 'react-native';

const App = () => {
  return (
    <SafeAreaView>
      <Button title={'Toggle'} />
    </SafeAreaView>
  );
};

export default App;
